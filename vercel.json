{
    "version": 2,
    "builds": [
        {
            "src": "./src/strava_server/server.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/src/strava_server/server.py"
        }
    ]
}